<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.suryansgoel.dine24">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".LoggedinActivity" />
        <activity android:name=".ControlActivity" />
        <activity android:name=".DltbookActivity" />
        <activity android:name=".SlctuserActivity" />
        <activity android:name=".AddresActivity" />
        <activity android:name=".RestaurantDetailsActivity" />
    </application>

</manifest>